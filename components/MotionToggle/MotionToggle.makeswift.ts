import { runtime } from '@/lib/makeswift/runtime'
import MotionToggle from './MotionToggle'

runtime.registerComponent(MotionToggle, {
  type: 'motion-toggle',
  label: 'Motion Toggle',
  props: {},
})
